# Production daemon config — optimized portfolio (2026-03-27)
# Cut underperformers, concentrate capital on proven winners
# Source: 90d grid-search + walk-forward optimization
# Wallets: 10 (from 15), capital weighted by Sharpe ratio

[trading]
exchange = "upbit"
symbol = "KRW-BTC"
symbols = ["KRW-BTC", "KRW-ETH", "KRW-XRP", "KRW-SOL"]
interval = "minute60"
candle_count = 200
paper_trading = true

[strategy]
momentum_lookback = 12
momentum_entry_threshold = 0.001
momentum_exit_threshold = -0.01
bollinger_window = 20
bollinger_stddev = 1.8
rsi_period = 14
rsi_oversold_floor = 20.0
rsi_recovery_ceiling = 75.0
rsi_overbought = 72.0
max_holding_bars = 36
adx_threshold = 20.0

[regime]
short_lookback = 10
long_lookback = 30
bull_threshold_pct = 0.03
bear_threshold_pct = -0.03

[drift]
bull_return_tolerance_pct = 0.15
sideways_return_tolerance_pct = 0.08
bear_return_tolerance_pct = 0.05
bull_error_rate_threshold = 0.25
sideways_error_rate_threshold = 0.20
bear_error_rate_threshold = 0.10

[risk]
risk_per_trade_pct = 0.01
stop_loss_pct = 0.03
take_profit_pct = 0.08
max_daily_loss_pct = 0.03
max_concurrent_positions = 3
trailing_stop_pct = 0.0
atr_stop_multiplier = 2.0
min_entry_confidence = 0.45
partial_tp_pct = 0.5
cooldown_bars = 3
max_position_pct = 0.20

[kill_switch]
max_portfolio_drawdown_pct = 0.08
max_daily_loss_pct = 0.03
max_consecutive_losses = 4
max_strategy_drawdown_pct = 0.06
cooldown_minutes = 120

[backtest]
initial_capital = 1000000.0
fee_rate = 0.0005
slippage_pct = 0.0005

[telegram]
bot_token = ""
chat_id = ""

[runtime]
log_level = "INFO"
poll_interval_seconds = 60
max_iterations = 0
daemon_mode = true
healthcheck_path = "artifacts/health.json"
runtime_checkpoint_path = "artifacts/runtime-checkpoint.json"
backtest_baseline_path = "artifacts/backtest-baseline.json"
regime_report_path = "artifacts/regime-report.json"
drift_calibration_path = "artifacts/drift-calibration.json"
operator_report_path = "artifacts/operator-report.md"
strategy_run_journal_path = "artifacts/strategy-runs.jsonl"
paper_trade_journal_path = "artifacts/paper-trades.jsonl"
position_snapshot_path = "artifacts/positions.json"
daily_performance_path = "artifacts/daily-performance.json"
drift_report_path = "artifacts/drift-report.json"
promotion_gate_path = "artifacts/promotion-gate.json"
daily_memo_path = "artifacts/daily-memo.md"
strategy_report_path = "artifacts/strategy-report.md"

# === TIER 1: Flagship winners (heavy allocation) ===

# momentum_sol: Sharpe 2.23, Return +2.69%, WR 59%, PF 1.77
[[wallets]]
name = "momentum_sol_wallet"
strategy = "momentum"
initial_capital = 2_000_000.0
symbols = ["KRW-SOL"]
[wallets.strategy_overrides]
momentum_lookback = 15
momentum_entry_threshold = 0.002
rsi_period = 14
rsi_overbought = 72.0
max_holding_bars = 36
adx_threshold = 20.0
volume_filter_mult = 1.2
[wallets.risk_overrides]
stop_loss_pct = 0.03
take_profit_pct = 0.08
risk_per_trade_pct = 0.015
partial_tp_pct = 0.5
max_concurrent_positions = 2
max_position_pct = 0.25

# momentum_eth: Sharpe 1.64, Return +0.94%, WR 43%, PF 1.47
[[wallets]]
name = "momentum_eth_wallet"
strategy = "momentum"
initial_capital = 1_500_000.0
symbols = ["KRW-ETH"]
[wallets.strategy_overrides]
momentum_lookback = 15
momentum_entry_threshold = 0.003
rsi_period = 14
rsi_overbought = 70.0
max_holding_bars = 36
adx_threshold = 15.0
volume_filter_mult = 1.0
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.04
risk_per_trade_pct = 0.015
atr_stop_multiplier = 3.0
partial_tp_pct = 0.5
max_concurrent_positions = 2

# vpin_sol: Sharpe 1.60, Return +2.13%, WR 58%, PF 1.57
[[wallets]]
name = "vpin_sol_wallet"
strategy = "vpin"
initial_capital = 1_500_000.0
symbols = ["KRW-SOL"]
[wallets.strategy_overrides]
rsi_period = 14
momentum_lookback = 10
max_holding_bars = 36
vpin_low_threshold = 0.45
vpin_high_threshold = 0.75
vpin_momentum_threshold = 0.0005
vpin_rsi_ceiling = 78.0
vpin_rsi_floor = 22.0
bucket_count = 20
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.04
risk_per_trade_pct = 0.015
atr_stop_multiplier = 3.0
partial_tp_pct = 0.5
max_concurrent_positions = 2

# vbreak_xrp: Sharpe 1.50, Return +1.39%, WR 47%, PF 1.45
[[wallets]]
name = "vbreak_xrp_wallet"
strategy = "volatility_breakout"
initial_capital = 1_500_000.0
symbols = ["KRW-XRP"]
[wallets.strategy_overrides]
k_base = 0.40
noise_lookback = 15
ma_filter_period = 5
max_holding_bars = 36
adx_threshold = 25.0
volume_filter_mult = 1.2
[wallets.risk_overrides]
stop_loss_pct = 0.03
take_profit_pct = 0.08
risk_per_trade_pct = 0.01
partial_tp_pct = 0.5
max_concurrent_positions = 2

# === TIER 2: Solid performers (moderate allocation) ===

# vpin_btc: Sharpe 1.23, Return +0.87%, WR 55%, PF 1.50
[[wallets]]
name = "vpin_btc_wallet"
strategy = "vpin"
initial_capital = 1_000_000.0
symbols = ["KRW-BTC"]
[wallets.strategy_overrides]
rsi_period = 14
momentum_lookback = 12
max_holding_bars = 48
vpin_low_threshold = 0.45
vpin_high_threshold = 0.75
vpin_momentum_threshold = 0.001
vpin_rsi_ceiling = 78.0
vpin_rsi_floor = 22.0
bucket_count = 20
[wallets.risk_overrides]
stop_loss_pct = 0.04
take_profit_pct = 0.06
risk_per_trade_pct = 0.01
trailing_stop_pct = 0.04
partial_tp_pct = 0.5

# momentum_btc: Sharpe 1.03, Return +0.54%, WR 56%, PF 1.36
[[wallets]]
name = "momentum_btc_wallet"
strategy = "momentum"
initial_capital = 1_000_000.0
symbols = ["KRW-BTC"]
[wallets.strategy_overrides]
momentum_lookback = 12
momentum_entry_threshold = 0.001
rsi_period = 14
rsi_overbought = 72.0
max_holding_bars = 36
adx_threshold = 20.0
volume_filter_mult = 1.2
[wallets.risk_overrides]
stop_loss_pct = 0.04
take_profit_pct = 0.08
risk_per_trade_pct = 0.008
partial_tp_pct = 0.5

# momentum_xrp: Sharpe 0.96, Return +0.69%, WR 53%, PF 1.25
[[wallets]]
name = "momentum_xrp_wallet"
strategy = "momentum"
initial_capital = 1_000_000.0
symbols = ["KRW-XRP"]
[wallets.strategy_overrides]
momentum_lookback = 12
momentum_entry_threshold = 0.001
rsi_period = 14
rsi_overbought = 70.0
max_holding_bars = 36
adx_threshold = 15.0
volume_filter_mult = 1.0
[wallets.risk_overrides]
stop_loss_pct = 0.03
take_profit_pct = 0.06
risk_per_trade_pct = 0.01
partial_tp_pct = 0.5

# === TIER 3: Niche / low-frequency (small allocation) ===

# volspike_btc: Sharpe 3.01, Return +1.11%, WR 71%, PF 7.76 (only 7 trades)
[[wallets]]
name = "volspike_btc_wallet"
strategy = "volume_spike"
initial_capital = 500_000.0
symbols = ["KRW-BTC"]
[wallets.strategy_overrides]
spike_mult = 3.0
volume_window = 20
min_body_ratio = 0.3
momentum_lookback = 12
rsi_period = 14
rsi_overbought = 72.0
max_holding_bars = 36
adx_threshold = 20.0
[wallets.risk_overrides]
stop_loss_pct = 0.03
take_profit_pct = 0.08
risk_per_trade_pct = 0.01
partial_tp_pct = 0.5

# kimchi_premium: Multi-symbol, Sharpe 0.08 avg but ETH 1.11
[[wallets]]
name = "kimchi_premium_wallet"
strategy = "kimchi_premium"
initial_capital = 500_000.0
symbols = ["KRW-BTC", "KRW-ETH", "KRW-SOL"]
[wallets.strategy_overrides]
rsi_period = 14
rsi_recovery_ceiling = 45.0
rsi_overbought = 75.0
max_holding_bars = 24
min_trade_interval_bars = 6
min_confidence = 0.3
cooldown_hours = 8.0
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.04
atr_stop_multiplier = 3.0
partial_tp_pct = 0.5
cooldown_bars = 5

# vpin_eth: Sharpe 0.83, Return +0.43%, PF 1.28 — smallest allocation
[[wallets]]
name = "vpin_eth_wallet"
strategy = "vpin"
initial_capital = 500_000.0
symbols = ["KRW-ETH"]
[wallets.strategy_overrides]
rsi_period = 14
momentum_lookback = 10
max_holding_bars = 36
vpin_low_threshold = 0.45
vpin_high_threshold = 0.75
vpin_momentum_threshold = 0.0005
vpin_rsi_ceiling = 78.0
vpin_rsi_floor = 22.0
bucket_count = 20
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.06
risk_per_trade_pct = 0.01
partial_tp_pct = 0.5
cooldown_bars = 5

# === Capital Allocation Summary ===
# TIER 1 (Sharpe > 1.5):
#   momentum_sol:    2,000,000 (18.2%) — Sharpe 2.23
#   momentum_eth:    1,500,000 (13.6%) — Sharpe 1.64
#   vpin_sol:        1,500,000 (13.6%) — Sharpe 1.60
#   vbreak_xrp:     1,500,000 (13.6%) — Sharpe 1.50
# TIER 2 (Sharpe 0.8-1.5):
#   vpin_btc:        1,000,000 (9.1%)  — Sharpe 1.23
#   momentum_btc:    1,000,000 (9.1%)  — Sharpe 1.03
#   momentum_xrp:    1,000,000 (9.1%)  — Sharpe 0.96
# TIER 3 (niche):
#   volspike_btc:      500,000 (4.5%)  — Sharpe 3.01 (low freq)
#   kimchi_premium:    500,000 (4.5%)  — Sharpe 0.08 avg
#   vpin_eth:          500,000 (4.5%)  — Sharpe 0.83
# TOTAL:          11,000,000
#
# REMOVED (underperformers):
#   vbreak_btc (Sharpe -0.92), vbreak_eth (0.48 weak), volspike_eth (-0.21)
#   vpin_xrp (Sharpe -1.29), mean_rev_eth (historically bad)
#   consensus_btc (no grid data), ema_cross_btc (no grid data)

[credentials]
upbit_access_key = ""
upbit_secret_key = ""
