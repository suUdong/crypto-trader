# Production daemon config — per-symbol optimized 6-wallet portfolio
# Deployed: 2026-03-26
# Wallets: momentum_btc(2.51) + momentum_eth(4.86) + kimchi(1.22) + vpin_btc(3.40) + vpin_eth(2.05) + vpin_sol(2.55)
# All per-symbol Sharpe > 1.0 | Total 90d backtest return: ~+54% across 6 wallets

[trading]
exchange = "upbit"
symbol = "KRW-BTC"
symbols = ["KRW-BTC", "KRW-ETH", "KRW-XRP", "KRW-SOL"]
interval = "minute60"
candle_count = 200
paper_trading = true

# Global strategy params — tuned for trade frequency + profitability balance
[strategy]
momentum_lookback = 15
momentum_entry_threshold = 0.001
momentum_exit_threshold = -0.01
bollinger_window = 20
bollinger_stddev = 1.8
rsi_period = 18
rsi_oversold_floor = 20.0
rsi_recovery_ceiling = 75.0
rsi_overbought = 72.0
max_holding_bars = 36
adx_threshold = 20.0

[regime]
short_lookback = 10
long_lookback = 30
bull_threshold_pct = 0.03
bear_threshold_pct = -0.03

[drift]
bull_return_tolerance_pct = 0.15
sideways_return_tolerance_pct = 0.08
bear_return_tolerance_pct = 0.05
bull_error_rate_threshold = 0.25
sideways_error_rate_threshold = 0.20
bear_error_rate_threshold = 0.10

# Risk — momentum walk-forward fold 2 optimal
[risk]
risk_per_trade_pct = 0.01
stop_loss_pct = 0.03
take_profit_pct = 0.10
max_daily_loss_pct = 0.05
max_concurrent_positions = 4
trailing_stop_pct = 0.0
atr_stop_multiplier = 0.0
min_entry_confidence = 0.25

[backtest]
initial_capital = 1000000.0
fee_rate = 0.0005
slippage_pct = 0.0005

[telegram]
bot_token = ""
chat_id = ""

[runtime]
log_level = "INFO"
poll_interval_seconds = 60
max_iterations = 0
daemon_mode = true
healthcheck_path = "artifacts/health.json"
runtime_checkpoint_path = "artifacts/runtime-checkpoint.json"
backtest_baseline_path = "artifacts/backtest-baseline.json"
regime_report_path = "artifacts/regime-report.json"
drift_calibration_path = "artifacts/drift-calibration.json"
operator_report_path = "artifacts/operator-report.md"
strategy_run_journal_path = "artifacts/strategy-runs.jsonl"
paper_trade_journal_path = "artifacts/paper-trades.jsonl"
position_snapshot_path = "artifacts/positions.json"
daily_performance_path = "artifacts/daily-performance.json"
drift_report_path = "artifacts/drift-report.json"
promotion_gate_path = "artifacts/promotion-gate.json"
daily_memo_path = "artifacts/daily-memo.md"
strategy_report_path = "artifacts/strategy-report.md"

# --- Wallets (per-symbol optimized) ---

[[wallets]]
name = "momentum_btc_wallet"
strategy = "momentum"
initial_capital = 1_000_000.0
symbols = ["KRW-BTC"]
# BTC-optimized: Sharpe 2.51, Return +1.82%, WR 56.9%, PF 1.68, 51 trades
[wallets.strategy_overrides]
momentum_lookback = 15
momentum_entry_threshold = 0.002
rsi_period = 14
rsi_overbought = 72.0
max_holding_bars = 48
adx_threshold = 20.0
[wallets.risk_overrides]
stop_loss_pct = 0.04
take_profit_pct = 0.08
risk_per_trade_pct = 0.008

[[wallets]]
name = "momentum_eth_wallet"
strategy = "momentum"
initial_capital = 1_000_000.0
symbols = ["KRW-ETH"]
# ETH-optimized: Sharpe 4.86, Return +15.14%, WR 53.9%, PF 3.22, 76 trades
[wallets.strategy_overrides]
momentum_lookback = 12
momentum_entry_threshold = 0.002
rsi_period = 14
rsi_overbought = 72.0
max_holding_bars = 48
adx_threshold = 20.0
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.04
risk_per_trade_pct = 0.015
atr_stop_multiplier = 3.0

[[wallets]]
name = "kimchi_premium_wallet"
strategy = "kimchi_premium"
initial_capital = 1_000_000.0
symbols = ["KRW-BTC", "KRW-ETH", "KRW-XRP", "KRW-SOL"]
# Per-wallet overrides from 90-day grid search (Sharpe 1.22, WR 51%, PF 1.49)
[wallets.strategy_overrides]
rsi_period = 14
rsi_recovery_ceiling = 50.0
rsi_overbought = 75.0
max_holding_bars = 24
min_trade_interval_bars = 18
min_confidence = 0.4
cooldown_hours = 8.0
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.04
atr_stop_multiplier = 3.0

# --- VPIN wallets (per-symbol optimized, all Sharpe > 2.0) ---

[[wallets]]
name = "vpin_btc_wallet"
strategy = "vpin"
initial_capital = 1_000_000.0
symbols = ["KRW-BTC"]
# BTC-optimized: Sharpe 3.40, Return +6.63%, WR 53%, PF 2.38, 59 trades
[wallets.strategy_overrides]
rsi_period = 14
momentum_lookback = 12
max_holding_bars = 48
vpin_low_threshold = 0.55
vpin_high_threshold = 0.80
vpin_momentum_threshold = 0.001
vpin_rsi_ceiling = 78.0
vpin_rsi_floor = 22.0
bucket_count = 20
[wallets.risk_overrides]
stop_loss_pct = 0.04
take_profit_pct = 0.06
risk_per_trade_pct = 0.015
trailing_stop_pct = 0.04

[[wallets]]
name = "vpin_eth_wallet"
strategy = "vpin"
initial_capital = 1_000_000.0
symbols = ["KRW-ETH"]
# ETH-optimized: Sharpe 2.05, Return +8.18%, WR 55%, PF 1.41, 75 trades
[wallets.strategy_overrides]
rsi_period = 14
momentum_lookback = 12
max_holding_bars = 48
vpin_low_threshold = 0.55
vpin_high_threshold = 0.80
vpin_momentum_threshold = 0.001
vpin_rsi_ceiling = 78.0
vpin_rsi_floor = 22.0
bucket_count = 20
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.06
risk_per_trade_pct = 0.01

[[wallets]]
name = "vpin_sol_wallet"
strategy = "vpin"
initial_capital = 1_000_000.0
symbols = ["KRW-SOL"]
# SOL-optimized: Sharpe 2.55, Return +7.85%, WR 63%, PF 1.91, 89 trades
[wallets.strategy_overrides]
rsi_period = 14
momentum_lookback = 12
max_holding_bars = 48
vpin_low_threshold = 0.55
vpin_high_threshold = 0.80
vpin_momentum_threshold = 0.001
vpin_rsi_ceiling = 78.0
vpin_rsi_floor = 22.0
bucket_count = 20
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.04
risk_per_trade_pct = 0.015
atr_stop_multiplier = 3.0

# --- Volatility breakout wallets (Larry Williams-style breakout) ---

[[wallets]]
name = "vbreak_btc_wallet"
strategy = "volatility_breakout"
initial_capital = 1_000_000.0
symbols = ["KRW-BTC"]
# BTC volatility breakout with tight k for trend-following
[wallets.strategy_overrides]
k_base = 0.40
noise_lookback = 15
ma_filter_period = 10
max_holding_bars = 36
[wallets.risk_overrides]
stop_loss_pct = 0.03
take_profit_pct = 0.08
risk_per_trade_pct = 0.01

[[wallets]]
name = "vbreak_eth_wallet"
strategy = "volatility_breakout"
initial_capital = 1_000_000.0
symbols = ["KRW-ETH"]
# ETH volatility breakout — wider range for higher volatility
[wallets.strategy_overrides]
k_base = 0.25
noise_lookback = 12
ma_filter_period = 8
max_holding_bars = 36
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.06
risk_per_trade_pct = 0.015
atr_stop_multiplier = 3.0

# --- Volume Spike wallets (institutional accumulation detection) ---

[[wallets]]
name = "volspike_btc_wallet"
strategy = "volume_spike"
initial_capital = 1_000_000.0
symbols = ["KRW-BTC"]
# BTC volume spike — lowered multiplier for sideways market signal frequency
[wallets.strategy_overrides]
spike_mult = 1.8
volume_window = 20
min_body_ratio = 0.4
momentum_lookback = 12
rsi_period = 14
rsi_overbought = 72.0
max_holding_bars = 48
adx_threshold = 20.0
[wallets.risk_overrides]
stop_loss_pct = 0.03
take_profit_pct = 0.08
risk_per_trade_pct = 0.01

[[wallets]]
name = "volspike_eth_wallet"
strategy = "volume_spike"
initial_capital = 1_000_000.0
symbols = ["KRW-ETH"]
# ETH volume spike — aggressive threshold for higher-vol asset in sideways market
[wallets.strategy_overrides]
spike_mult = 1.5
volume_window = 20
min_body_ratio = 0.35
momentum_lookback = 12
rsi_period = 14
rsi_overbought = 70.0
max_holding_bars = 36
adx_threshold = 18.0
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.06
risk_per_trade_pct = 0.015
atr_stop_multiplier = 3.0

# --- Consensus wallet (multi-strategy agreement filter) ---

[[wallets]]
name = "consensus_btc_wallet"
strategy = "consensus"
initial_capital = 1_000_000.0
symbols = ["KRW-BTC"]
# Enters when momentum, vpin, or volume_spike signals — wider net with multi-strategy exit safety
[wallets.strategy_overrides]
sub_strategies = ["momentum", "vpin", "volume_spike"]
weights = [1.0, 1.0, 0.8]
min_agree = 1
momentum_lookback = 12
momentum_entry_threshold = 0.0005
rsi_period = 14
rsi_recovery_ceiling = 70.0
adx_threshold = 12.0
max_holding_bars = 48
# VPIN sub-strategy: relaxed thresholds for consensus context
vpin_momentum_threshold = 0.001
vpin_low_threshold = 0.50
vpin_rsi_ceiling = 75.0
# Volume spike sub-strategy: lower spike for sideways market signal frequency
spike_mult = 1.5
min_body_ratio = 0.3
[wallets.risk_overrides]
stop_loss_pct = 0.03
take_profit_pct = 0.08
risk_per_trade_pct = 0.015

# --- EMA Crossover wallet (trend-following with EMA 9/21 crossover) ---

[[wallets]]
name = "ema_cross_btc_wallet"
strategy = "ema_crossover"
initial_capital = 1_000_000.0
symbols = ["KRW-BTC"]
# BTC EMA crossover — trend following with RSI confirmation
[wallets.strategy_overrides]
rsi_period = 14
rsi_overbought = 70.0
max_holding_bars = 48
[wallets.risk_overrides]
stop_loss_pct = 0.03
take_profit_pct = 0.08
risk_per_trade_pct = 0.01

# --- Mean Reversion wallet (Bollinger Band bounce) ---

[[wallets]]
name = "mean_rev_eth_wallet"
strategy = "mean_reversion"
initial_capital = 1_000_000.0
symbols = ["KRW-ETH"]
# ETH mean reversion — buy at lower BB, sell at upper BB
[wallets.strategy_overrides]
bollinger_window = 20
bollinger_stddev = 2.0
rsi_period = 14
rsi_oversold_floor = 25.0
rsi_recovery_ceiling = 55.0
max_holding_bars = 36
[wallets.risk_overrides]
stop_loss_pct = 0.02
take_profit_pct = 0.06
risk_per_trade_pct = 0.01

[credentials]
upbit_access_key = ""
upbit_secret_key = ""
