# No validated deployment config
# Date: 2026-03-26
# Source: artifacts/walk-forward-90d/fixed-params-summary.json
# Result: all 7 optimized candidates failed fixed-parameter walk-forward validation

[validation]
status = "failed"
deployable = false
dataset_days = 90
strategy_count = 7
symbols = ["KRW-BTC", "KRW-ETH", "KRW-XRP", "KRW-SOL"]
method = "fixed-parameter walk-forward over optimized 90d candidates"
gate = "avg_test_return_pct > 0, oos_win_rate >= 0.5, avg_efficiency_ratio > 0.3, majority-pass across symbols"
top_in_sample_candidate = "momentum"
top_in_sample_avg_sharpe = 1.34
artifact = "docs/walk-forward-results.md"
